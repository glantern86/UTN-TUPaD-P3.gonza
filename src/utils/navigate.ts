export const navigate = (route: string) => {
  window.location.href = route;
};

/* Tengo que chequear cual funciona, si la func anterior o esta
export function navigate(path: string): void {
  window.location.href = path;
}
*/
